<?xml version="1.0" encoding="UTF-8"?>
<definition xmlns="http://workflow.opencastproject.org">
  <id>attach-google-speech-transcripts</id>
  <title>Attach caption/transcripts generated by Google Speech</title>
  <tags />
  <description>Attach automated transcription generated by the Google speech service. This is an internal workflow, started by the Transcription Service.
  </description>
  <configuration_panel />

  <operations>

    <!-- Attach caption/transcript -->

    <operation id="attach-google-speech-transcription"
      fail-on-error="true"
      exception-handler-workflow="ng-partial-error" 
      description="Attach captions/transcription">
      <configurations>
        <!-- This is filled out by the transcription service when starting this workflow -->
        <configuration key="transcription-job-id">${transcriptionJobId}</configuration>
        <configuration key="target-flavor">captions/timedtext</configuration>
        <configuration key="target-tag">archive</configuration>
        <configuration key="target-caption-format">webvtt</configuration>
      </configurations>
    </operation>

    <!-- Re-publish to OAI-PMH -->

    <operation
      id="republish-oaipmh"
      exception-handler-workflow="ng-partial-error"
      description="REPUBLISH: Update recording in default OAI-PMH repository">
      <configurations>
        <configuration key="source-flavors">dublincore/*,security/*,captions/*</configuration>
        <configuration key="repository">default</configuration>
        <configuration key="merge">true</configuration>
      </configurations>
    </operation>

    <!-- Archive the current state of the mediapackage -->

    <operation
      id="archive"
      fail-on-error="true"
      exception-handler-workflow="manchester-archive-error"
      description="PUBLISH: Archive">
      <configurations>
        <configuration key="source-tags">archive</configuration>
      </configurations>
    </operation>


    <!-- Clean the system from work artifacts -->

    <operation
      id="include"
      description="&lt;&lt;&lt; Cleanup">
      <configurations>
        <configuration key="workflow-id">manchester-partial-cleanup</configuration>
      </configurations>
    </operation>

  </operations>

</definition>

