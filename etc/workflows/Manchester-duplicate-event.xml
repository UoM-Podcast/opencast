<?xml version="1.0" encoding="UTF-8" ?>
<definition xmlns="http://workflow.opencastproject.org">

  <id>manchester-duplicate-event</id>
  <title>Manchester Duplicate Event</title>
  <tags>
    <tag>archive-ng</tag>
  </tags>
  <description/>
  <configuration_panel>
    <![CDATA[
      <div id="workflow-configuration">
        <fieldset>
          <div>
            <label for="mpTitle" style="width: 150px;" >Title of Duplicate:</label>
            <input id="mpTitle" name="mpTitle" type="text" class="configField" style="width: 400px;" value=" " />
          </div>
          <br/>
          <div>
            <label for="seriesId" style="width: 150px;" >Series Id:</label>
            <input id="seriesId" name="seriesId" type="text" maxlength="255" class="configField" style="width: 400px;" value=" " />
          </div>
          <br/>
          <div>
            <label for="startDate" style="width: 150px;">StartDate:</label>
            <input id="startDate" name="startDate" type="date" class="configField" style="width: 200px;" value=" " />
            <input id="startTime" name="startTime" type="time" class="configField" style="width: 190px;" value=" " />
         </div>
       </fieldset>
      </div>
   ]]>
  </configuration_panel>

  <operations>

    <operation
      id="duplicate-event"
      fail-on-error="true"
      exception-handler-workflow="partial-error"
      description="Duplicate Event">
      <configurations>
        <configuration key="source-flavors">*/*</configuration>
        <configuration key="target-tags"></configuration>
        <configuration key="set-series-id">${seriesId}</configuration>
        <configuration key="set-title">${mpTitle}</configuration>
        <configuration key="set-start-date-time">${startDate} ${startTime}</configuration>
        <configuration key="copy-suffix">copy</configuration>
      </configurations>
    </operation>

    <operation id="start-workflow">
      <configurations>
        <configuration key="workflow-definition">manchester</configuration>
        <configuration key="media-package-key">duplicate_media_package_id</configuration>
      </configurations>
    </operation>

    <!-- Clean the system from work artifacts -->

    <operation
        id="cleanup"
        fail-on-error="false"
        description="Remove temporary processing artifacts">
      <configurations>
        <configuration key="delete-external">true</configuration>
        <!-- FixMe Don't clean up ACLs until workflow service no longer looks for them in the WFR. -->
        <configuration key="preserve-flavors">security/*</configuration>
      </configurations>
    </operation>

  </operations>

  <state-mappings>
    <state-mapping state="running">EVENTS.EVENTS.STATE_MAPPING.DUPLICATING</state-mapping>
    <state-mapping state="failing">EVENTS.EVENTS.STATE_MAPPING.DUPLICATING</state-mapping>
  </state-mappings>

</definition>
